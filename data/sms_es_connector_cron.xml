<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_cron_sms_queue_worker" model="ir.cron">
            <field name="name">SMS-ES: Procesar Cola de Envíos</field>
            <field name="model_id" ref="model_sms_es_queue_job"/>
            <field name="state">code</field>
            <field name="code">model._process_sms_queue()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">minutes</field>
            <!-- El campo 'numbercall' / 'number_of_calls' se establecerá mediante un hook  <field name="numbercall">-1</field> -->
            <!-- Los campos dependientes de la versión se establecen vía hook -->
            <!--<field name="doall" eval="False"/>-->
        </record>
    </data>
</odoo>